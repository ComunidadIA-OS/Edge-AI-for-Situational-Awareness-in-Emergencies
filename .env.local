# carlosModel.py loads this file via python-dotenv.
# The API key and URL are currently hard-coded in models/carlosModel.py,
# so nothing is strictly required here. Add overrides if you move them out.
# ROBOFLOW_API_KEY=
# ROBOFLOW_API_URL=http://localhost:9001
