{
  "name": "heimdall",
  "version": "0.1.0",
  "private": true,
  "description": "Edge AI for Situational Awareness in Emergencies — monorepo",
  "scripts": {
    "dev:ground-control": "pnpm --filter @heimdall/ground-control dev",
    "build:ground-control": "pnpm --filter @heimdall/ground-control build",
    "lint:ground-control": "pnpm --filter @heimdall/ground-control lint",
    "dev:edge": "pnpm --filter @heimdall/edge dev"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.4"
}
