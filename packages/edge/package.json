{
  "name": "@heimdall/edge",
  "version": "0.1.0",
  "private": true,
  "description": "Jetson edge detection — YOLO26 + TensorRT + FastAPI",
  "scripts": {
    "dev": "uvicorn convergence.api:app --host 0.0.0.0 --port 8000 --reload"
  }
}
