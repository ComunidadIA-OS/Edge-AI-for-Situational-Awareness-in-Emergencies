ignore-workspace=true
verify-deps-before-run=false
strict-dep-builds=false
ignore-scripts=true

# Network resilience: retry transient registry failures during the first
# Docker build on slow / flaky / proxied connections instead of aborting.
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=180000
